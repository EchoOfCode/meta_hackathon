# torch is pre-installed on Kaggle with CUDA support — do NOT reinstall it here.
# accelerate, transformers, datasets are also pre-installed; pinned for safety.

openenv

# core ML — versions pinned to match what the install log resolved successfully
unsloth==2026.4.8
unsloth_zoo==2026.4.9
trl==0.24.0
mergekit>=0.1.4
transformers==5.5.0
datasets==4.3.0
peft>=0.18.0           # required by unsloth's get_peft_model (LoRA)
bitsandbytes==0.49.2
accelerate>=1.12.0
huggingface_hub==1.12.0
xformers==0.0.35

# pinned to avoid the fsspec conflict from the install log
fsspec==2025.9.0

# experiment tracking
wandb

# standard data libs (pre-installed on Kaggle, listed for non-Kaggle environments)
numpy
pandas
matplotlib